# Contracts (Duplicated)

This project intentionally duplicates contracts (not a monorepo or shared package).
If a contract changes, update **all three** copies (api/admin/public).

Main contract files:
- index.ts
